---
phase: 01-publishable-portfolio-and-delivery
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - astro.config.ts
  - playwright.config.ts
  - tests/skeleton.spec.ts
  - tsconfig.json
  - src/data/profile.ts
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
  - src/styles/global.css
autonomous: false
requirements:
  - PROF-01
must_haves:
  truths:
    - "A visitor can load the built home page and read William Castle's confirmed introduction and computer science graduate background without JavaScript."
    - "The first page is deliberately composed as a warm-ivory exhibition catalogue while remaining normal semantic document flow."
    - "The project installs only the Phase 1 Astro/checking stack after the suspicious-package review gate and produces a reproducible lockfile."
  artifacts:
    - path: package.json
      provides: "Pinned build and browser-check commands"
    - path: tests/skeleton.spec.ts
      provides: "Walking-skeleton browser contract"
    - path: src/pages/index.astro
      provides: "Static profile route"
    - path: src/data/profile.ts
      provides: "Confirmed public identity constants"
    - path: src/styles/global.css
      provides: "Phase 1 visual tokens, responsive composition, focus treatment, and reduced-motion policy"
  key_links:
    - from: src/pages/index.astro
      to: src/data/profile.ts
      via: "imports and renders the confirmed profile"
      pattern: "profile\.(displayName|introduction|background)"
    - from: tests/skeleton.spec.ts
      to: src/pages/index.astro
      via: "Playwright checks the built page's visible identity and initial HTML"
      pattern: "William Castle"
    - from: src/pages/index.astro
      to: src/styles/global.css
      via: "BaseLayout imports the global catalogue stylesheet used by the home route"
      pattern: "global\.css"
---

## Phase Goal

**As a** prospective employer, **I want to** open a readable portfolio and immediately understand who William is, **so that** I can begin assessing his work without waiting for an immersive scene.

<objective>
Create the Phase 1 walking skeleton: a reproducible Astro static build with a failing browser contract first, followed by a real profile page that makes the contract pass.

Purpose: Establish the content-to-static-HTML-to-browser path while implementing the confirmed identity and visual direction from D-01, D-02, D-03, D-12, and D-13. Keep direct destinations visible in the composition per D-04, but do not invent the unsupplied resume or contact values governed by D-14 and D-15.
Output: Pinned project scaffold, lockfile, browser harness, confirmed profile data, semantic layout, and the built home page.
</objective>

<execution_context>
@/home/castlewr/.codex/gsd-core/workflows/execute-plan.md
@/home/castlewr/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-publishable-portfolio-and-delivery/01-CONTEXT.md
@.planning/phases/01-publishable-portfolio-and-delivery/01-RESEARCH.md
@.planning/phases/01-publishable-portfolio-and-delivery/01-PATTERNS.md
@.planning/phases/01-publishable-portfolio-and-delivery/01-SKELETON.md
</context>

<scope_rationale>
This walking-skeleton plan owns ten exact files with no wildcard path groups. The RED task changes five toolchain/test files plus one neutral route scaffold; the GREEN task reuses that route and changes four additional application files. Keeping them in one plan preserves the required RED-to-GREEN vertical slice while each implementation task remains within the documented six-file medium-task bound.
</scope_rationale>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify recently published package identities before installation</name>
  <read_first>
    - .planning/phases/01-publishable-portfolio-and-delivery/01-RESEARCH.md (Package Legitimacy Audit and Standard Stack)
    - .planning/phases/01-publishable-portfolio-and-delivery/01-PATTERNS.md (project foundation assignment)
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>Before any npm install, present the official npm package pages and upstream repositories for `astro@7.3.3` and `@playwright/test@1.63.0`, the two packages marked SUS only because of recent publication. Also show the audited OK packages `typescript@6.0.3`, `@astrojs/check@0.9.10`, and `@axe-core/playwright@4.13.0`. Record the human's explicit approval; do not install on silence, and do not substitute similarly named packages.</action>
  <verify>
    <automated>npm view astro@7.3.3 name version repository.url dist.integrity && npm view @playwright/test@1.63.0 name version repository.url dist.integrity</automated>
  </verify>
  <acceptance_criteria>
    - Registry output identifies `astro` from `withastro/astro` and `@playwright/test` from `microsoft/playwright` at the audited versions.
    - The user explicitly approves both flagged packages before Task 2 runs.
  </acceptance_criteria>
  <done>The package-legitimacy gate is approved and the exact install set is known.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold the reproducible build and write the failing browser contract</name>
  <read_first>
    - .planning/phases/01-publishable-portfolio-and-delivery/01-RESEARCH.md (Standard Stack, Build once/test once/deploy once, Code Examples)
    - .planning/phases/01-publishable-portfolio-and-delivery/01-PATTERNS.md (root config and browser-test assignments)
    - .planning/phases/01-publishable-portfolio-and-delivery/01-CONTEXT.md (D-12 and D-13)
  </read_first>
  <files>package.json, package-lock.json, astro.config.ts, playwright.config.ts, tests/skeleton.spec.ts, src/pages/index.astro</files>
  <behavior>
    - Test 1: built `/` exposes a level-one heading named William Castle and the exact confirmed introduction.
    - Test 2: the same content remains visible in a browser context with JavaScript disabled.
    - Test 3: the page exposes discoverable Projects, About, Resume, and Contact destinations in semantic navigation.
  </behavior>
  <action>Initialize npm in the existing repository without overwriting `.planning/`; install the exact approved pins from Task 1, then immediately run `npx playwright install --with-deps chromium`, commit `package-lock.json`, and set Node `>=24 <25`. Add scripts for `astro check`, `astro build`, Playwright against the built output, and the combined check path. Configure Astro static output with `site: 'https://castlew640.github.io'` and no `base`. Configure Playwright's web server to serve the built `dist/`. Write `tests/skeleton.spec.ts` before the profile implementation; give the profile/navigation assertion the explicit diagnostic `PROFILE_NAV_CONTRACT_MISSING`. Then create only a neutral `src/pages/index.astro` build scaffold with none of the required identity/navigation copy so Astro build and preview infrastructure are valid. Run the focused test once and capture that exact RED diagnostic. Do not install React, Three.js, React Three Fiber, or any scene dependency in this phase.</action>
  <verify>
    <automated>npm run build &amp;&amp; npx playwright install --with-deps chromium &amp;&amp; if npm run test:e2e -- tests/skeleton.spec.ts &gt;/tmp/phase-01-skeleton-red.log 2&gt;&amp;1; then exit 1; fi &amp;&amp; grep -Fq 'PROFILE_NAV_CONTRACT_MISSING' /tmp/phase-01-skeleton-red.log</automated>
  </verify>
  <acceptance_criteria>
    - `package-lock.json` resolves only the approved Phase 1 direct dependencies at their pinned versions.
    - `astro.config.ts` contains the account-site origin and no project-site base prefix.
    - The initial skeleton test run fails because the required profile/navigation is absent, not because of a test syntax or configuration error.
  </acceptance_criteria>
  <done>The reproducible scaffold exists and a valid failing end-to-end contract defines the first user-visible slice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make the static profile skeleton pass with the exhibition-catalogue composition</name>
  <read_first>
    - tests/skeleton.spec.ts
    - astro.config.ts
    - .planning/phases/01-publishable-portfolio-and-delivery/01-CONTEXT.md (D-01 through D-04 and D-12 through D-15)
    - .planning/phases/01-publishable-portfolio-and-delivery/01-PATTERNS.md (profile, layout, route, and CSS assignments)
  </read_first>
  <files>tsconfig.json, src/data/profile.ts, src/layouts/BaseLayout.astro, src/pages/index.astro, src/styles/global.css</files>
  <behavior>
    - Test 1: confirmed name, introduction, and broad graduate background render in initial HTML.
    - Test 2: semantic navigation links target visible `projects`, `about`, `resume`, and `contact` sections in DOM order.
    - Test 3: the built route contains no client-side script needed to expose essential profile content.
  </behavior>
  <action>Extend Astro's strict TypeScript preset. Export `profile` from `src/data/profile.ts` with `displayName`, exact `introduction`, and broad `background`; model resume/contact destinations as absent until the owner supplies them, rather than adding examples. Build `BaseLayout.astro` with descriptive metadata, canonical URL support, skip link, header/nav, main, and footer. Build `/` from normal HTML sections and mark unsupplied publication actions as incomplete editorial state without fake links. Create CSS tokens and responsive composition implementing warm ivory, dark readable ink, expressive locally safe typography, construction-line geometry, and progressive “plans becoming places” details per D-01 through D-03. Keep content in normal flow; include durable `:focus-visible`, fluid type/spacing, and no canvas, fixed-width desktop shell, hover-only disclosure, animation dependency, or Phase 2 code. Run GREEN and retain the test.</action>
  <verify>
    <automated>npm run check && npm run build && npm run test:e2e -- tests/skeleton.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/profile.ts` contains `William Castle`, the exact D-13 sentence, and a computer-science-graduate background with no job specialization claim.
    - `dist/index.html` contains the profile and four direct section destinations without requiring JavaScript.
    - No source file imports React, Three.js, React Three Fiber, or a canvas component.
    - The skeleton Playwright suite passes against the built artifact.
  </acceptance_criteria>
  <done>A real visitor can open the static portfolio, understand William's confirmed positioning, and navigate its readable structure.</done>
</task>

</tasks>

## Artifacts this phase produces

- `package.json` — scripts `check`, `build`, and `test:e2e`; pinned direct dependencies and Node policy.
- `package-lock.json` — reproducible dependency graph.
- `astro.config.ts` — static account-site origin configuration.
- `playwright.config.ts` — built-output browser server and test configuration.
- `tests/skeleton.spec.ts` — walking-skeleton profile/no-JavaScript/navigation contract.
- `tsconfig.json` — strict Astro TypeScript configuration.
- `src/data/profile.ts` — exported `profile` object with confirmed identity fields and explicit absent publication fields.
- `src/layouts/BaseLayout.astro` — metadata, skip link, semantic shell, and canonical link contract.
- `src/pages/index.astro` — static home/profile route and direct section targets.
- `src/styles/global.css` — Phase 1 tokens, architectural catalogue composition, focus, and reflow rules.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry → local build | Third-party packages enter the public site's build chain. |
| typed profile data → generated HTML | Publication data becomes public static output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Information Disclosure | `src/data/profile.ts` | mitigate | Store only confirmed public identity; absent contact/resume values remain absent. |
| T-01-02 | Tampering | generated metadata/links | mitigate | Use Astro URL generation and a fixed canonical account-site origin; forbid arbitrary raw HTML. |
| T-01-SC | Tampering | npm installs | mitigate | Block installation until the human verifies the two SUS package identities; commit the exact npm lockfile. |
</threat_model>

<verification>
- `npm run check`, `npm run build`, and `npm run test:e2e -- tests/skeleton.spec.ts` pass after the GREEN implementation.
- `dist/index.html` is readable as HTML and contains no invented public destination.
- Dependency inspection shows no Phase 2 rendering packages.
</verification>

<success_criteria>
- The walking skeleton proves local confirmed data becomes static HTML and survives a real browser journey.
- PROF-01 is observable in the built page.
- The visual foundation implements D-01 through D-04 without leaking the Phase 2 corridor into Phase 1.
</success_criteria>

<output>
Create `.planning/phases/01-publishable-portfolio-and-delivery/01-01-SUMMARY.md` when done.
</output>
