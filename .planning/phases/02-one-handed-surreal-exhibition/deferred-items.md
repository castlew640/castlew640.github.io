# Deferred Items — Phase 02

- **Pre-existing schema deprecation hint:** `astro check` reports `z.string().url()` in `src/content.config.ts:14` as deprecated. The existing HTTPS validation still passes; migration is unrelated to the exhibition catalogue and was not included in plan 02-02. Validation reports 0 errors, 0 warnings, and 1 hint.
