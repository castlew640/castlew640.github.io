---
phase: 03-growth-and-release-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-project-growth.mjs
  - playwright.growth.config.ts
  - tests/fixtures/projects/entries.mjs
  - tests/project-growth.spec.ts
  - src/content.config.ts
  - src/lib/project-schema.ts
  - src/lib/project-validation.ts
  - src/pages/projects/[...slug].astro
  - src/components/exhibition/ExhibitSection.astro
  - tests/project-validation.test.ts
  - docs/project-starter/index.md
  - docs/AUTHORING.md
autonomous: true
requirements: [GROW-01, GROW-04]
must_haves:
  truths:
    - "An owner can copy the starter, add actual evidence, and publish a personal project through the existing page, catalogue, and exhibit path."
    - "Visitors can read short case studies, compact showcases, and technical deep dives with appropriate repository or demo links."
    - "The existing client story still has its three distinct contracts and approved evidence."
    - "New work follows existing exhibits while earlier slugs and order remain stable."
  artifacts:
    - path: src/content.config.ts
      provides: Discriminated client and personal project collection
    - path: docs/project-starter/index.md
      provides: Copyable unpublished Markdown starter outside the loader
    - path: scripts/check-project-growth.mjs
      provides: Isolated content-only growth verification
    - path: tests/project-growth.spec.ts
      provides: Canonical page, index, return, and publication-boundary contracts
  key_links:
    - from: src/content.config.ts
      to: src/lib/project-validation.ts
      via: getPublishedProjects validates the complete collection before filtering and sorting
    - from: src/pages/projects/[...slug].astro
      to: src/components/exhibition/ExhibitSection.astro
      via: Both consume the same validated published entry and stable slug
---

## Phase Goal

**As a** portfolio owner, **I want to** extend the exhibition with completed projects while preserving a polished, responsive experience, **so that** visitors can explore my growing work through a reliable live application link.

<objective>
Deliver flexible content-to-page-to-exhibit publishing and a short authoring path per D-01, D-02, D-03, D-05 and D-28. Keep the detailed client format as its own validated branch. This is the content slice; optional video and image-fit work follow in 03-02.
Purpose: New completed work must require content and assets, without bespoke rendering or navigation edits.
Output: Flexible publication, a copyable starter, and an isolated growth contract.
</objective>

<execution_context>
@/home/castlewr/.codex/gsd-core/workflows/execute-plan.md
@/home/castlewr/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-growth-and-release-polish/03-CONTEXT.md
@.planning/phases/03-growth-and-release-polish/03-RESEARCH.md
@.planning/phases/03-growth-and-release-polish/03-PATTERNS.md
@src/lib/projects.ts
@src/content/projects/featured-client/index.md
@scripts/verify-built-content.mjs

Existing interfaces: getPublishedProjects() calls validateProjectRecords(entries.map(({id,data}) => ({id,...data}))), filters data.published, then sorts by exhibitionOrder. The route renders Markdown through render(project). ExhibitSection receives project: CollectionEntry<'projects'> and index: number, and emits data-slug plus /#exhibit-{slug}. Preserve these shared gateways.
</context>

## Artifacts this phase produces

- New files: scripts/check-project-growth.mjs, playwright.growth.config.ts, tests/fixtures/projects/entries.mjs, tests/project-growth.spec.ts, docs/project-starter/index.md, docs/AUTHORING.md.
- New project fields: kind ('client' | 'personal', default 'client' for existing entries), format ('case-study' | 'showcase' | 'deep-dive', default 'case-study'), repositoryUrl (optional HTTPS), evidence (personal image records with src, alt, caption, kind: screenshot/terminal/diagram/illustration, fit: contain/cover).
- New reusable schemas: httpsUrlSchema, evidenceSchema; extend ProjectRecord to the discriminated publication contract. Existing client screenshots, liveUrl, contracts, slug and exhibitionOrder retain their meanings.
- New fixture CLI: node scripts/check-project-growth.mjs --count 0|1|10 --suite growth|media|performance; --count defaults to 10, --suite defaults to growth. Later plans implement the additional suites. A temporary report includes its absolute root and output paths; fixture output never uses the main dist directory.

<tasks>
<task type="auto">
  <name>Task 1: Prove a content-only personal project journey with a failing isolated browser contract</name>
  <read_first>src/lib/projects.ts, src/content.config.ts, src/pages/projects/[...slug].astro, playwright.config.ts, tests/portfolio.spec.ts, scripts/verify-built-content.mjs</read_first>
  <files>scripts/check-project-growth.mjs, playwright.growth.config.ts, tests/fixtures/projects/entries.mjs, tests/project-growth.spec.ts</files>
  <action>Create a bounded fixture runner per D-05 that copies the current working source/config/tests/public assets into a fresh OS temporary directory, excludes .git/dist/.astro/node_modules and reports its location, and resolves the existing installed dependencies without installing packages. Build and preview only that copy on port 4322; use a dedicated Playwright config and always stop the child server. Hash the main dist before and after and require equality. Keep fixture entries and assets outside the collection and public directories in the real checkout. Generate count 0, 1, or 10 scenarios, retaining the real client first whenever the count is positive and adding repository-only personal entries for growth; supply a separate personal-only happy-path scenario inside the suite. Use copied approved images as test data and unmistakable fixture sentinels, never public accomplishments. First write and run the browser contract that a supplied personal Markdown entry produces its title/summary, canonical page, catalogue exhibit, relevant repository link, and stable return anchor while a draft does not. Capture its expected failure against the client-only schema. The runner invokes the temporary build and selected tests, not the main aggregate check. No production source changes belong in this task.</action>
  <acceptance_criteria>A nonzero run reports a personal-project publication failure; it is not a missing binary, preview bind, or import-setup failure. The checkout's content and dist hashes are unchanged. Empty/single/expanded fixtures have separate temporary roots and cannot be uploaded by the Pages workflow.</acceptance_criteria>
  <verify><automated>node scripts/check-project-growth.mjs --count 10 --suite growth</automated></verify>
  <done>The failing end-to-end contract and isolated fixture runner are usable without a real personal-project prerequisite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish flexible personal projects through the real collection and both visitor surfaces</name>
  <read_first>src/content.config.ts, src/lib/project-schema.ts, src/lib/project-validation.ts, src/lib/projects.ts, src/pages/projects/[...slug].astro, src/components/exhibition/ExhibitSection.astro, tests/project-growth.spec.ts</read_first>
  <files>src/content.config.ts, src/lib/project-schema.ts, src/lib/project-validation.ts, src/pages/projects/[...slug].astro, src/components/exhibition/ExhibitSection.astro</files>
  <behavior>Published personal entries with a real still and honest Markdown render without contracts or a live demo. Supplied links must be HTTPS. Existing clients require exactly one website/manual-planner/ai-mvp contract, approved screenshots and their live URL. Duplicate published slugs and exhibitionOrder fail.</behavior>
  <action>Implement the smallest discriminated schema and render branches per D-02, D-03, D-05 and D-28. Preserve existing client records through the default client discriminator; do not edit their narrative or screenshots. Personal entries use evidence, optional repositoryUrl/liveUrl and format; require at least one still with nonblank alt/caption when published, not an invented hosted demo. Validate all supplied URLs, even on draft entries, and require unique nonnegative published exhibitionOrder alongside slug uniqueness. Astro image() resolves local stills; keep additional publication checks in validateProjectRecords. Branch the page's eyebrow/links, evidence gallery and client contract section; render Markdown unchanged so the selected format supports short, compact or detailed narrative. Branch ExhibitSection's first image and contract labels; preserve visible title/summary/caption, the exact Read case study → anchor, stable data-slug and existing published-only source. Expose evidence kind/fit as escaped DOM data attributes for 03-02. Case-study routes and exhibit metadata must not serialize draft bodies, links, or assets.</action>
  <acceptance_criteria>The Task 1 personal journey passes. A repository-only project has no empty live-demo control or client-contract heading. The featured client still has its three approved figures and distinct contract records. Existing project URLs and ordering remain unchanged.</acceptance_criteria>
  <verify><automated>node scripts/check-project-growth.mjs --count 10 --suite growth</automated></verify>
  <done>Adding a valid personal content folder creates all three public surfaces without renderer/navigation changes.</done>
</task>

<task type="auto">
  <name>Task 3: Make repeatable authoring clear and reject invalid publication inputs</name>
  <read_first>src/lib/project-validation.ts, tests/project-validation.test.ts, src/content/projects/featured-client/index.md, .planning/phases/03-growth-and-release-polish/03-CONTEXT.md</read_first>
  <files>tests/project-validation.test.ts, docs/project-starter/index.md, docs/AUTHORING.md</files>
  <action>Complete validation edge cases for both branches, duplicate published order, invalid supplied HTTPS URLs, missing stills, and published-versus-draft slug behavior. Create the starter folder and index.md outside the loader/public boundary per D-01. Use published: false, clearly instructional fields, and section prompts for What it does, Why it exists, Decisions, Results and Evidence; explain removing sections for showcase or adding Architecture, Tradeoffs, Experiments and Lessons for deep-dive per D-02. The guide provides copy/edit/add-real-media, choose a stable slug, choose an order greater than the current maximum, npm run check, review diff, and normal git commit/push commands with expected results. Explain that a non-hosted tool can use terminal/diagram evidence and a repository link. Draft media stays outside public. Preserve old slug/order values on later edits. Reserve a clearly named optional-media subsection for the concrete 03-02 contract; authoring a still-only project is complete now. No project-generator CLI, new dependency, or forced project/video production.</action>
  <acceptance_criteria>The short guide matches the actual schema and names the public release guide as docs/RELEASE.md (created by 03-08). Copying the starter and supplying real values/assets passes; untouched instructional content remains unpublished. Node validation covers each client invariant and personal publication failure.</acceptance_criteria>
  <verify><automated>node --experimental-strip-types --test tests/project-validation.test.ts</automated></verify>
  <done>The owner can author the next real entry independently and invalid inputs fail before publication.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authored content → public HTML | Publication status, links, and local image references enter the build. |
| Fixture runner → checked artifact | Artificial content must remain isolated from dist and deployment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Tampering | project schema/validator | mitigate | Discriminated validation, HTTPS parsing, duplicate slug/order rejection and Astro-escaped output. |
| T-03-02 | Information disclosure | fixtures/drafts | mitigate | OS temporary builds, source outside loader/public, production artifact hashes unchanged, published-only gateway. |
| T-03-03 | Repudiation | client narrative | mitigate | Retain confirmed three-contract text and approved evidence unchanged. |
</threat_model>

<verification>
Run focused validation and isolated growth tests, then npm run build and node scripts/verify-built-content.mjs against production output. N=0/1/10 fixture results are testing evidence; the production catalogue still contains only real published entries. Aggregate check wiring is owned by 03-07 to avoid parallel package.json edits.
</verification>

<success_criteria>Content plus assets is sufficient to publish all supported narrative formats; clients retain their stronger guarantees; templates, drafts and fixtures do not appear in production.</success_criteria>

<output>Create .planning/phases/03-growth-and-release-polish/03-01-SUMMARY.md with fixture commands, publication guarantees, and any execution deviations.</output>
