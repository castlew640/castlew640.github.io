---
phase: 03-growth-and-release-polish
plan: "02"
type: execute
wave: 2
depends_on: ["03-01", "03-03"]
files_modified:
  - tests/project-growth.spec.ts
  - tests/project-media.test.ts
  - tests/fixtures/projects/entries.mjs
  - tests/fixtures/projects/silent-demo.mp4
  - src/lib/project-schema.ts
  - src/content.config.ts
  - src/lib/project-media.ts
  - src/lib/projects.ts
  - src/pages/projects/[...slug].astro
  - src/components/exhibition/ExhibitSection.astro
  - src/scripts/exhibition/scene/exhibit.ts
  - src/styles/global.css
  - scripts/verify-built-content.mjs
  - docs/AUTHORING.md
autonomous: true
requirements: [GROW-01, PERF-01]
must_haves:
  truths:
    - "Visitors can read an entire terminal capture or diagram in both still and 3D exhibits."
    - "A project may offer a native video that starts only after the visitor activates playback."
    - "The gallery uses a still preview even when its project page has a video."
    - "Only media belonging to published projects reaches the public artifact."
  artifacts:
    - path: src/lib/project-media.ts
      provides: Contained published-media filesystem validation
      exports: [validatePublicMedia]
    - path: tests/project-media.test.ts
      provides: Media containment, ownership and accessibility metadata tests
  key_links:
    - from: src/lib/projects.ts
      to: src/lib/project-media.ts
      via: Validate publication ownership before returning published entries
    - from: src/components/exhibition/ExhibitSection.astro
      to: src/scripts/exhibition/scene/exhibit.ts
      via: Shared optimized image element, evidence fit and four-corner panel geometry
---

## Phase Goal

**As a** portfolio owner, **I want to** extend the exhibition with completed projects while preserving a polished, responsive experience, **so that** visitors can explore my growing work through a reliable live application link.

<objective>
Deliver honest media choices and optional page-only video per D-03, D-04, D-05 and D-28. Preserve readable evidence across aspect ratios and keep media publication safe.
Purpose: A CLI, library, diagram-led project, or app should have suitable evidence without inventing a hosted demo.
Output: Contained image fit, a native video contract, validation and authoring instructions.
</objective>

<execution_context>
@/home/castlewr/.codex/gsd-core/workflows/execute-plan.md
@/home/castlewr/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-growth-and-release-polish/03-CONTEXT.md
@.planning/phases/03-growth-and-release-polish/03-RESEARCH.md
@.planning/phases/03-growth-and-release-polish/03-PATTERNS.md
@.planning/phases/03-growth-and-release-polish/03-01-SUMMARY.md
@.planning/phases/03-growth-and-release-polish/03-03-SUMMARY.md
@src/lib/projects.ts
@src/scripts/exhibition/scene/exhibit.ts

Consume 03-01's kind/format/evidence/repositoryUrl contract and isolated runner. Consume 03-03's panel local-group placement and world-corner projection; do not reintroduce world-z assumptions. Existing textures wrap the decoded DOM img, use SRGBColorSpace, and own their cleanup.
</context>

## Artifacts this phase produces

- New files: src/lib/project-media.ts, tests/project-media.test.ts.
- New schemas/fields: videoSchema and optional project.video with src, poster, description, hasAudio, captions ({src, language, label} when hasAudio is true). poster uses Astro image() and is an authored local still. Existing evidence.fit controls image fit.
- New API: validatePublicMedia(entries, publicRoot), asynchronous, invoked by getPublishedProjects; checks real filesystem containment and publication ownership.
- New rendered hooks: data-evidence-kind and data-evidence-fit; native video, track, description and download link on canonical pages only. Fixture runner's --suite media becomes executable here.
- New binary fixture: tests/fixtures/projects/silent-demo.mp4, a tiny self-authored silent H.264/MP4 with provenance and SHA-256 recorded in entries.mjs. Tests copy it only into the temporary fixture root. Poster/VTT and varied-ratio images are also fixture-only; no public project video is introduced.

<tasks>
<task type="auto">
  <name>Task 1: Write failing page-media and uncropped-evidence journeys</name>
  <read_first>tests/project-growth.spec.ts, scripts/check-project-growth.mjs, tests/fixtures/projects/entries.mjs, src/components/exhibition/ExhibitSection.astro, src/pages/projects/[...slug].astro</read_first>
  <files>tests/project-growth.spec.ts, tests/project-media.test.ts, tests/fixtures/projects/entries.mjs, tests/fixtures/projects/silent-demo.mp4</files>
  <action>Extend the isolated fixture suite before production changes. Render deterministic wide-terminal, portrait-diagram and ordinary screenshot PNGs using the installed Playwright browser. Add a small self-authored silent baseline H.264/MP4 fixture depicting a plain geometric shape; record its authoring method, duration, codec and SHA-256 in entries.mjs and verify decoded frames before using it. Check in the encoded fixture under tests so Linux/CI never needs an encoder and no owner video is required; do not assume Chromium can encode MP4. Copy the fixture and generated poster/VTT only into the temporary build. Assert a supplied page video is paused initially, has controls/playsinline/poster/preload=none, starts after activation, provides a visible description and download link, and has a caption track when hasAudio is true. Test first and fourth image corners remain visible under contain fit, DOM and texture use the same optimized URL, and video URLs never occur in home/scene metadata. Add failing filesystem tests for traversal, encoded separators, symlink escape, missing files, orphan public media, and draft-only media; include an audio case lacking captions. Compare home network requests before playback without treating preload as a universal zero-byte promise.</action>
  <acceptance_criteria>Focused media tests fail because the requested media behavior is absent. The fixture can decode its checked-in MP4 in Chromium; this is explicitly browser-fixture evidence, not iPhone decoding evidence.</acceptance_criteria>
  <verify><automated>node scripts/check-project-growth.mjs --count 10 --suite media</automated></verify>
  <done>A real visitor journey and containment failures define the new optional-media contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offer accessible native project video with a strict public-media boundary</name>
  <read_first>src/lib/project-schema.ts, src/content.config.ts, src/lib/projects.ts, src/pages/projects/[...slug].astro, tests/project-media.test.ts</read_first>
  <files>src/lib/project-schema.ts, src/content.config.ts, src/lib/project-media.ts, src/lib/projects.ts, src/pages/projects/[...slug].astro</files>
  <behavior>Still-only entries remain valid. Published video requires a local poster and description; meaningful audio requires VTT captions. Only safe existing MP4/VTT files referenced by published entries may exist below public/media.</behavior>
  <action>Implement D-04 with native video controls, playsinline, preload="none", an Astro-resolved poster, visible description, and a normal download link. Playback requires visitor action; the video lives only on the project page. Extend image() at the collection boundary for poster and keep filesystem work in validatePublicMedia. Limit video.src to /media/{stable-slug}/{safe-name}.mp4 and captions.src to an equivalent .vtt path; reject query/fragment, percent-encoded path syntax, dot segments, backslashes, mismatched slug ownership, symlinks leaving the public-media root, missing files and files over 25,000,000 bytes. Require nonempty WebVTT content for caption records and nonempty description for silent demonstrations; hasAudio requires captions language and label. Audit every public/media file against the published whitelist before Astro copies public, including orphan/draft files. Keep draft media outside public as the guide directs. Existing image references and client publication rules remain intact. No new player package, provider, external embed requirement or actual portfolio video is needed.</action>
  <acceptance_criteria>The native player works with keyboard and pointer; page text remains useful without playback/JS. Draft/orphan/traversal media fails the build. No home video element, source URL or video texture is emitted.</acceptance_criteria>
  <verify><automated>node --experimental-strip-types --test tests/project-media.test.ts</automated></verify>
  <done>Optional video is a complete accessible page feature with publishable-file ownership enforced before output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve complete evidence in optimized stills and route-mounted panels</name>
  <read_first>src/components/exhibition/ExhibitSection.astro, src/scripts/exhibition/scene/exhibit.ts, src/styles/global.css, scripts/verify-built-content.mjs, docs/AUTHORING.md, .planning/phases/03-growth-and-release-polish/03-UI-AMENDMENT.md</read_first>
  <files>src/components/exhibition/ExhibitSection.astro, src/scripts/exhibition/scene/exhibit.ts, src/styles/global.css, scripts/verify-built-content.mjs, docs/AUTHORING.md</files>
  <behavior>Terminal/diagram images always contain their full extent. Ordinary screenshots may cover only when at most 10% of either source dimension is cropped. Every image is at most 1600px on its long edge and uses one shared DOM/GPU asset.</behavior>
  <action>Implement D-03/D-04 media fit in both DOM and scene. Bound getImage output by both dimensions, preserve ratio, and use the same img for the WebGL texture. Force contain for terminal/diagram or explicit contain; fall back to contain whenever a 2:1 cover crop would discard more than 10%. Use stone-lit letterboxing inside the existing 4 by 2 metre evidence frame; scale the actual textured plane without stretching and leave frame/picking coherent with 03-03's world-corner projection. Preserve alt/caption and failed-image recovery. Mark distant DOM images lazy while keeping the entrance's first real evidence promptly available. Extend built-output verification to reject fixture/template/draft sentinels and media not in the published whitelist. Finish AUTHORING's exact optional-video/file/description/captions and evidence-fit instructions; instruct authors to verify supplied video on the real target browsers before publishing it. Respect the existing 180,000-byte per-preview budget; recompress actual assets rather than changing limits.</action>
  <acceptance_criteria>Portrait and wide evidence remain complete and legible; image corners and picking agree after route transforms. Production output contains no fixture video or media sentinels. Client screenshots and all three evidence figures remain present.</acceptance_criteria>
  <verify><automated>node scripts/check-project-growth.mjs --count 10 --suite media</automated></verify>
  <done>Appropriate real stills and optional video publish through the same content-only authoring workflow.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Content paths → filesystem/public artifact | Public files bypass Astro draft filtering unless audited. |
| Project video → browser | Untrusted metadata becomes native media attributes and links. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-04 | Information disclosure | validatePublicMedia | mitigate | Resolve real paths, reject traversal/escape, enforce published slug ownership and complete public/media whitelist. |
| T-03-05 | Denial of service | local media | mitigate | Validate file limits, optimize stills, page-only preload-none native playback. |
| T-03-06 | Tampering | media markup | mitigate | Schema validation and escaped Astro attributes; HTTPS remains required for external project links. |
</threat_model>

<verification>Run isolated media/browser and filesystem cases, npm run build, and node scripts/verify-built-content.mjs. Verify the final production home contains only still evidence. Project video is optional; no owner video task is a release prerequisite.</verification>

<success_criteria>All selected project types have honest readable evidence; optional native media is usable and contained; drafts and fixtures cannot leak via public.</success_criteria>

<output>Create .planning/phases/03-growth-and-release-polish/03-02-SUMMARY.md with media contract, fixture results, and any target-browser evidence limitations.</output>
