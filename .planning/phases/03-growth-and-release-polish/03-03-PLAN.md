---
phase: 03-growth-and-release-polish
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-growth-and-release-polish/03-UI-AMENDMENT.md
  - tests/exhibition-path.test.ts
  - tests/exhibition-route.spec.ts
  - src/scripts/exhibition/scene/path.ts
  - src/scripts/exhibition/scene/index.ts
  - src/scripts/exhibition/controller.ts
  - src/scripts/exhibition/scene/architecture.ts
  - src/scripts/exhibition/scene/exhibit.ts
  - tests/exhibition-stops.test.ts
  - tests/exhibition-travel.spec.ts
  - tests/exhibition-navigation.spec.ts
  - tests/exhibition-resilience.spec.ts
  - tests/exhibition-budget.spec.ts
autonomous: true
requirements: [GROW-04, PERF-01, PERF-02]
must_haves:
  truths:
    - "Native scrolling carries visitors gently left/right and up/down while the horizon stays level."
    - "At each exhibit, visitors see a calm complete evidence panel and can open its canonical page."
    - "Appending work preserves the world pose, URL and identity of every earlier exhibit."
    - "Returning from a project restores its understandable stop and keyboard focus."
  artifacts:
    - path: src/scripts/exhibition/scene/path.ts
      provides: Deterministic fixed-segment route frames
      exports: [sampleRoute, routeBounds]
    - path: .planning/phases/03-growth-and-release-polish/03-UI-AMENDMENT.md
      provides: Explicit Phase 3 replacement values for inherited UI assumptions
    - path: tests/exhibition-route.spec.ts
      provides: End-to-end spatial navigation and panel projection contract
  key_links:
    - from: src/scripts/exhibition/controller.ts
      to: src/scripts/exhibition/scene/index.ts
      via: Measured native scroll supplies scalar station and stable stopId through setTravel
    - from: src/scripts/exhibition/scene/path.ts
      to: src/scripts/exhibition/scene/architecture.ts
      via: Shared frames place the walkway and surrounding geometry
    - from: src/scripts/exhibition/scene/exhibit.ts
      to: src/scripts/exhibition/scene/index.ts
      via: Actual transformed panel corners and panel-only raycasting
---

## Phase Goal

**As a** portfolio owner, **I want to** extend the exhibition with completed projects while preserving a polished, responsive experience, **so that** visitors can explore my growing work through a reliable live application link.

<objective>
Deliver the navigable winding route per D-12, D-13, D-14, D-27 and D-28, preserving native-scroll and content-growth contracts. Reconcile obsolete design/test assumptions explicitly. 03-04 then strengthens architectural warping, transformations and completed-world reflection on this shared frame.
Purpose: The camera and the space must agree, rather than simulating a winding journey with camera sway.
Output: A working route, stable readable exhibits, spatial contracts and updated navigation assertions.
</objective>

<execution_context>
@/home/castlewr/.codex/gsd-core/workflows/execute-plan.md
@/home/castlewr/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-growth-and-release-polish/03-CONTEXT.md
@.planning/phases/03-growth-and-release-polish/03-RESEARCH.md
@.planning/phases/03-growth-and-release-polish/03-PATTERNS.md
@.planning/phases/02-one-handed-surreal-exhibition/02-UI-SPEC.md
@.planning/phases/02-one-handed-surreal-exhibition/02-MEASUREMENTS.md
@src/lib/exhibition/stops.ts
@src/scripts/exhibition/scroll.ts
@src/scripts/exhibition/scene/index.ts

Existing stop.z and ScrollProgress.z are deterministic signed scalar values: entrance 0, exhibits -18n, About -18N-18, landing -18N-28. Preserve those tested layout values; convert once at the scene boundary to positive station = -progress.z. They cease to represent world camera coordinates. No Three.js import belongs outside scene/. SceneHandle currently exposes setCameraZ; replace it with setTravel(station, stopId) while retaining hitPanel/resize/dispose.
</context>

## Artifacts this phase produces

- New files: scene/path.ts, tests/exhibition-path.test.ts, tests/exhibition-route.spec.ts, 03-UI-AMENDMENT.md at the paths above.
- New route APIs/types: RouteFrame {position, forward, right, up}, sampleRoute(station), routeBounds(maxStation). Use existing Three.js curve/vector classes.
- Scene interface changes: SceneHandle.setTravel(station, stopId); SceneOptions.landingStation and travel {station, stopId} replace landingStopZ/cameraZ. Existing scalar stop tables and browser controls remain authoritative.
- New diagnostics: station, currentStopId, cameraYaw, cameraRoll, cameraUpX/Y/Z, panelCorner0–3X/Y, panelFacingCamera and panelStopId; widen the existing diagnostic value type to include strings. Existing camera/world/count diagnostics retain accurate meanings.

<tasks>
<task type="auto">
  <name>Task 1: Specify and fail the winding-route visitor contract</name>
  <read_first>.planning/phases/02-one-handed-surreal-exhibition/02-UI-SPEC.md, .planning/phases/02-one-handed-surreal-exhibition/02-MEASUREMENTS.md, .planning/phases/03-growth-and-release-polish/03-CONTEXT.md, tests/exhibition-travel.spec.ts, tests/exhibition-stops.test.ts</read_first>
  <files>.planning/phases/03-growth-and-release-polish/03-UI-AMENDMENT.md, tests/exhibition-path.test.ts, tests/exhibition-route.spec.ts</files>
  <action>Write the active Phase 3 amendment per D-27 without rewriting earlier UAT results. Name replaced sections A/B/C/D/F/G/K and copy inventory; all unaffected typography, palette, lights, ink, input/failure and resource limits remain inherited. Establish fixed 18m segments independent of N: first 12m straight and level, final 6m a CubicBezierCurve3 transition. Repeat lateral anchor offsets [0, 0.8, 0, -0.8]m and elevation offsets [0, 0.30, 0.10, 0]m by segment index; compute control points from that fixed pattern even beyond the current end. Use control z spacing of 2m with start/end x/y repeated to make horizontal/vertical derivatives zero at joins. Eye height is 1.62m above the route datum, walkway is datum+0.12m, yaw stays within 12 degrees, world up is (0,1,0), pitch/roll zero. Exhibit mounting is 12m ahead of its stop, 4 by 2m evidence frame at datum+4.1m; local clear walkway width remains 6m. Landing at +10m after About lies in a calm straight section. Specify opaque paper labels, 4.5:1 text contrast, phone policy/copy/glass values from 03-05 and unchanged numeric budgets, as the later tasks' design contract. Write browser tests that native travel yields lateral/elevation change, level horizon, complete four-corner framing, usable actual panel picks and stable returns, then run them to show the current straight route fails. Pure path tests cover segment continuity, finite clamps and append invariance.</action>
  <acceptance_criteria>The amendment is explicit about supersession, not a claim of new human approval. The browser test fails on absent winding behavior. Path fixtures use N=0/1/2/10 and compare earlier frames, not total-curve-normalized positions.</acceptance_criteria>
  <verify><automated>npm run test:e2e -- tests/exhibition-route.spec.ts --workers=1</automated></verify>
  <done>Concrete route geometry and visitor outcomes are testable before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Navigate a shared route with mounted architecture and pickable evidence</name>
  <read_first>src/scripts/exhibition/controller.ts, src/scripts/exhibition/scene/index.ts, src/scripts/exhibition/scene/architecture.ts, src/scripts/exhibition/scene/exhibit.ts, src/scripts/exhibition/scroll.ts, .planning/phases/03-growth-and-release-polish/03-UI-AMENDMENT.md</read_first>
  <files>src/scripts/exhibition/scene/path.ts, src/scripts/exhibition/scene/index.ts, src/scripts/exhibition/controller.ts, src/scripts/exhibition/scene/architecture.ts, src/scripts/exhibition/scene/exhibit.ts</files>
  <behavior>The same scroll station always produces the same pose in both directions. Every earlier exhibit pose is identical after appending. Camera roll/pitch remain zero, panel corners remain inside the viewport at each stop, and the actual panel opens the matching slug.</behavior>
  <action>Implement the amendment using Three's line/cubic curve primitives, fixed segment samples and a horizontal normalized forward vector per D-12/D-13. The controller passes station=-progress.z plus the measured stop's stable ID; preserve remeasurement, fractional arrival, hashes, Back/Forward, focus, blocked storage and async import generation guards. Clamp station between entrance and landing and derive camera position/yaw directly, without time-based smoothing. Use route frames for the continuous walkway mesh, vestibule/threshold, portal groups and landing so the actual path follows the camera. Preserve local foot clearance, the 12m exhibit standoff and unwarped evidence planes. Use stopId to select the current exhibit, update scene/camera matrixWorld before projection/picking, project all four actual local panel corners to screen, and compute min/max bounds from all four. Keep full captions in semantic HTML and provide numeric corner diagnostics rather than frame-driven DOM movement. Follow the current route frame with the existing sun/target offsets so shadows track the viewer. Preserve raycast-only-panels, anchor.click navigation, shared materials, partial-safe cleanup, demand rendering and the lazy boundary. Every geometry created or cloned is registered with its owner for disposal. Add the inherited N≥2 impossible-construction motif in createExhibits: render portal 2's right pier as drawn-only beneath its solid lintel/panel, with its solid completed counterpart available to layer 2. Keep that exception separate from outboard-member variation. This factory is owned here before 03-02 changes its media fit.</action>
  <acceptance_criteria>Full forward/back travel produces bounded winding/elevation and level horizon. At 1440×810, 390×664 and 844×390, each stop's four evidence corners stay on screen with top margin at least 5% and do not overlap its text plate. Client image taps and explicit/browser returns resolve correctly. At N≥2, portal 2's right pier is drawn-only beneath its solid lintel/panel, with a complete layer-2 counterpart; preserve the existing deterministic outboard-member variation.</acceptance_criteria>
  <verify><automated>node --experimental-strip-types --test tests/exhibition-path.test.ts</automated></verify>
  <done>A visitor can use the winding path end to end and select real work with the existing one-handed controls.</done>
</task>

<task type="auto">
  <name>Task 3: Replace superseded assertions while preserving one-handed regression coverage</name>
  <read_first>tests/exhibition-stops.test.ts, tests/exhibition-travel.spec.ts, tests/exhibition-navigation.spec.ts, tests/exhibition-resilience.spec.ts, tests/exhibition-budget.spec.ts, tests/exhibition-route.spec.ts</read_first>
  <files>tests/exhibition-stops.test.ts, tests/exhibition-travel.spec.ts, tests/exhibition-navigation.spec.ts, tests/exhibition-resilience.spec.ts, tests/exhibition-budget.spec.ts</files>
  <action>Replace only obsolete fixed x/y/world-z projection assertions per D-27. Assert scalar station, sampled world frame, zero roll/pitch, current stable stop and actual projected corners instead. Derive project/stop counts from the published DOM, preserve FOV computation, texture/URL reuse, tap rejection, endpoint rounding, canonical links, focus/history, failure recovery and independent idle/hidden suspension per D-28. Test one-panel and appended-panel identity without hardcoded one-project arrays. Retain the existing budget formulas and actual allocation reporting; winding geometry does not justify larger ceilings. Record aggregate test wiring as 03-07 ownership; run these suites directly now.</action>
  <acceptance_criteria>Old straight-axis assertions are replaced by stronger visible-route assertions. All existing input/accessibility/failure checks remain executable and still test their original behavior. No fake exhibit is inserted to make a test pass.</acceptance_criteria>
  <verify><automated>npm run test:e2e -- tests/exhibition-route.spec.ts tests/exhibition-travel.spec.ts tests/exhibition-navigation.spec.ts --workers=2</automated></verify>
  <done>Route regression coverage matches the authorized design and preserves previous visitor guarantees.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| DOM travel/slug → lazy scene | Native input selects a numeric route location and canonical project. |
| Scene mount/unmount → browser resources | Geometry, textures, observers and listeners need bounded ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-07 | Tampering | travel/picking | mitigate | Clamp finite station, resolve stable DOM stop IDs, and invoke only the matching existing canonical anchor. |
| T-03-08 | Denial of service | route/scene | mitigate | Fixed bounded segments, shared geometry/materials, demand rendering and idempotent partial-safe disposal. |
</threat_model>

<verification>Build before browser checks. Run route/stop tests, navigation/travel/resilience suites, scene-boundary and inherited byte/count checks. The complete world reflection/warp refinement is explicitly owned by 03-04; no physical-device pass is inferred here.</verification>

<success_criteria>Native travel, spatial framing and picking agree; earlier project frames survive append; scene lifecycle and ordinary HTML remain intact.</success_criteria>

<output>Create .planning/phases/03-growth-and-release-polish/03-03-SUMMARY.md documenting the route API, amendment and replaced assertions.</output>
