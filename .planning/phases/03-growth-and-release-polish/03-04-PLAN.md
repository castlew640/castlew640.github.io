---
phase: 03-growth-and-release-polish
plan: "04"
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - tests/exhibition-reflection.spec.ts
  - src/scripts/exhibition/scene/architecture.ts
  - src/scripts/exhibition/scene/transformations.ts
  - src/scripts/exhibition/scene/index.ts
  - src/scripts/exhibition/scene/reflection.ts
  - tests/exhibition-budget.spec.ts
autonomous: true
requirements: [GROW-04, PERF-01, PERF-02]
must_haves:
  truths:
    - "Visitors see ivory structures curve overhead while evidence and essential content remain calm and readable."
    - "Reflections show finished structures beneath the unfinished visible world throughout the winding exhibition."
    - "Reversing travel reverses emerging construction exactly, and stopping travel stops visual work."
    - "The reflection concept remains visible when the renderer uses its cheaper fallback."
  artifacts:
    - path: src/scripts/exhibition/scene/reflection.ts
      provides: Route-aware planar reflection coverage, fade and completed-edge fallback
    - path: tests/exhibition-reflection.spec.ts
      provides: Curved construction, reflection and reversible approach contracts
  key_links:
    - from: src/scripts/exhibition/scene/path.ts
      to: src/scripts/exhibition/scene/transformations.ts
      via: Shared frames place structures and scalar route distance drives approach
    - from: src/scripts/exhibition/scene/architecture.ts
      to: src/scripts/exhibition/scene/reflection.ts
      via: Completed transformed structure and meaningful world edges feed layer 2 and mirrored ink
---

## Phase Goal

**As a** portfolio owner, **I want to** extend the exhibition with completed projects while preserving a polished, responsive experience, **so that** visitors can explore my growing work through a reliable live application link.

<objective>
Realize D-10, D-11, D-12, D-13 and D-14 through curved-over architecture, coherent completed reflections, and reversible route-based transformations. Preserve lifecycle and budgets per D-26/D-28.
Purpose: Strengthen the surreal identity without compromising evidence, navigation or growth.
Output: A coordinated warped world with bounded reflection and resource ownership.
</objective>

<execution_context>
@/home/castlewr/.codex/gsd-core/workflows/execute-plan.md
@/home/castlewr/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-growth-and-release-polish/03-CONTEXT.md
@.planning/phases/03-growth-and-release-polish/03-RESEARCH.md
@.planning/phases/03-growth-and-release-polish/03-PATTERNS.md
@.planning/phases/03-growth-and-release-polish/03-03-SUMMARY.md
@.planning/phases/03-growth-and-release-polish/03-UI-AMENDMENT.md
@src/scripts/exhibition/scene/reflection.ts
@src/scripts/exhibition/scene/transformations.ts

Use sampleRoute(station) and routeBounds(maxStation) from 03-03. Existing createCompletedGroup(sources, stone, inks) builds layer-2 geometry; createReflection owns it after successful creation. Existing approachProgress uses smoothstep of distance 16→8m. Retain the ownership handoff and thresholds, replacing world-z distance with scalar station distance.
</context>

## Artifacts this phase produces

- New file: tests/exhibition-reflection.spec.ts.
- New deformation function: warpArchitecturePoint(localPoint, station, side), used for solid members, ink and completed counterparts; architecture-owned only.
- New transformation field/API: elements[].station and update(station), replacing z-based distance semantics; diagnostic transformationNStation.
- New reflection inputs: route bounds and transformed completed edge segments; routeDistance geometry attribute for fade, waterBounds diagnostics and completedEdgeCount/fallbackEdgeCount.
- New route-aware diagnostics: maximum structural bend, local walkway clearance, shadow target offset and water containment. All reflect actual geometry rather than hardcoded pass values.

<tasks>
<task type="auto">
  <name>Task 1: Fail the curved-world and completed-reflection journey</name>
  <read_first>tests/exhibition-budget.spec.ts, src/scripts/exhibition/scene/architecture.ts, src/scripts/exhibition/scene/reflection.ts, src/scripts/exhibition/scene/transformations.ts, .planning/phases/03-growth-and-release-polish/03-UI-AMENDMENT.md</read_first>
  <files>tests/exhibition-reflection.spec.ts</files>
  <action>Write browser contracts per D-11/D-14 for visible upper-structure deformation, local walkway clearance, unchanged evidence planes, transformed completed edges mirrored across y=0, route-relative reflection fade, and water coverage through the final landing. Exercise live reflector and forced small-surface/quality fallback, including restoration. Test the three approach transformations at stations producing progress 0, partial and 1, then reverse to the same station and compare geometry/progress/shadow state. Verify renderCount stops increasing after one second idle at partial progress. Keep test hooks tied to actual positions/matrices/bounds, and save review captures as test output rather than public portfolio evidence.</action>
  <acceptance_criteria>The existing straight structural shapes and world-x reflection fallback fail the new tests. All checks retain the inherited layer mask 4, one-reflector and one-shadow-light constraints.</acceptance_criteria>
  <verify><automated>npm run test:e2e -- tests/exhibition-reflection.spec.ts --workers=1</automated></verify>
  <done>Tests identify concrete composition and reflection behavior before changing the world.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bend architecture and resolve construction along the shared path</name>
  <read_first>src/scripts/exhibition/scene/architecture.ts, src/scripts/exhibition/scene/transformations.ts, src/scripts/exhibition/scene/index.ts, src/scripts/exhibition/scene/path.ts, .planning/phases/03-growth-and-release-polish/03-UI-AMENDMENT.md</read_first>
  <files>src/scripts/exhibition/scene/architecture.ts, src/scripts/exhibition/scene/transformations.ts, src/scripts/exhibition/scene/index.ts</files>
  <behavior>Architecture curves while panel evidence stays planar. Solids, outlines and completed forms share deformation. Transformation progress is reversible by station and casting starts at progress 0.5.</behavior>
  <action>Implement D-11's curve-over/warp in surrounding roof ribs, upper arches and outboard structures, with up to 0.8m inward bend above local y=3m, smoothly reaching its maximum by y=7m; alternate bend direction by fixed segment index. Add only enough vertical subdivisions to produce a visible curve, recompute normals/bounds after deformation and build ink from the same resulting geometry. Keep the six-metre walkway, footings, evidence frames and calm landing clear. Preserve ivory/ink palette, built-versus-drawn distinctions, the floating cornice on four drawn supports, and the landing's drawn-only right pier. Use station distances for the three existing approach elements, apply shared route placement to their visible and completed groups, and retain exact smoothstep 16→8m, shadow threshold 0.5, and clock-free reversal. The second exhibit's drawn-only right-pier motif is provided by 03-03's portal factory; assert it when N≥2 without modifying that factory in parallel with 03-02. Reuse batching/materials and register all created geometries with existing disposal owners.</action>
  <acceptance_criteria>Actual geometry has measurable curvature while evidence remains unwarped. The impossible constructions remain at their prescribed content counts. Shadow direction follows the route with the inherited light offsets and exactly one shadow-casting light.</acceptance_criteria>
  <verify><automated>npm run test:e2e -- tests/exhibition-reflection.spec.ts --grep "construction|approach|idle" --workers=1</automated></verify>
  <done>The winding visit has visibly curved architecture and coherent reversible emerging construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reflect the completed curved world across the full growing route</name>
  <read_first>src/scripts/exhibition/scene/reflection.ts, src/scripts/exhibition/scene/index.ts, tests/exhibition-budget.spec.ts, tests/exhibition-reflection.spec.ts, src/scripts/exhibition/scene/path.ts</read_first>
  <files>src/scripts/exhibition/scene/reflection.ts, src/scripts/exhibition/scene/index.ts, tests/exhibition-budget.spec.ts, tests/exhibition-reflection.spec.ts</files>
  <behavior>Fallback edges preserve world orientation and mirror y only. Water surrounds all route stations and the landing arch. Reflection fades by distance from the route, retains layer 2 and releases its target when disabled.</behavior>
  <action>Complete D-11/D-26 reflection integration using one planar water surface at y=0. Build completed geometry and meaningful silhouette/datum edges after world transforms; reflect each actual edge's y coordinate for fallback instead of deriving world-axis boxes. Derive water extents from routeBounds through landingStation+36m with 30m lateral and 20m end margins. Compute nearest horizontal route distance at water vertices from a bounded sampled centerline and interpolate a routeDistance attribute in the shader: strength 0.42 at 3m, linear fade to zero at 26m. Keep the existing 64×256 fallback gradient, layer-mask 4, all three lights on layers 0 and 2, one reflection pass, capped target, failure checks and reversible release/recreate behavior. Keep meaningful silhouettes within inherited line/draw/material limits by sharing/batching and selecting structural edges. Extend count tests to content-derived N and include fallback edges in line totals, real GPU allocations, the existing unattributed allocation disclosure and zero target after teardown. Subdivide the water plane into a regular grid with cells no larger than 2×2m, including interior vertices near both sides of the route, before computing routeDistance. A four-corner plane cannot represent this fade. Include the added triangles in the existing N-based ceiling and sample mid-cell rendered pixels as well as vertex values: completed structures must remain visibly reflected alongside the walkway at intermediate route stations. Compare the live reflection capture with the forced fallback capture, not only the plane bounds.</action>
  <acceptance_criteria>Completed structures and fallback ink align with curved visible architecture throughout the route. Water contains all tested stations and rendered reflection remains visible beside the path, including between grid vertices. No reflection target or scene-owned resource accumulates over repeated quality transitions; limits are unchanged.</acceptance_criteria>
  <verify><automated>npm run test:e2e -- tests/exhibition-reflection.spec.ts tests/exhibition-budget.spec.ts --workers=2</automated></verify>
  <done>Both reflection modes preserve the completed-world concept on a growing winding route.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authored content count → geometry | More exhibits expand geometry and reflection coverage. |
| WebGL allocation → lifecycle | Targets and cloned geometry require deterministic ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-09 | Denial of service | geometry/reflection | mitigate | Bounded samples, shared materials, one capped target, measured N-based budgets and idempotent disposal. |
| T-03-10 | Tampering | diagnostics | mitigate | Derive visibility/clearance/resource assertions from actual matrices, bounds and renderer information. |
</threat_model>

<verification>Build, run reflection/count contracts and node scripts/measure-scene-budget.mjs. Inspect desktop captures at entrance/exhibit/About/landing for coherent ink and calm evidence. This agent inspection does not close owner composition or physical-device acceptance.</verification>

<success_criteria>Curve-over architecture, route placement, transformations, shadow following and both reflection modes agree within the inherited budgets.</success_criteria>

<output>Create .planning/phases/03-growth-and-release-polish/03-04-SUMMARY.md with geometry/reflection results and remaining physical evidence.</output>
