---
phase: 03-growth-and-release-polish
plan: "07"
type: execute
wave: 5
depends_on: ["03-06"]
files_modified:
  - .planning/phases/03-growth-and-release-polish/03-PERFORMANCE-TARGETS.md
  - tests/performance-release.spec.ts
  - tests/fixtures/projects/entries.mjs
  - scripts/measure-exhibition.mjs
  - src/scripts/exhibition/scene/index.ts
  - src/scripts/exhibition/scene/exhibit.ts
  - src/scripts/exhibition/controller.ts
  - tests/exhibition-resilience.spec.ts
  - scripts/measure-scene-budget.mjs
  - scripts/check-project-growth.mjs
  - package.json
  - .planning/phases/03-growth-and-release-polish/03-MEASUREMENTS.md
autonomous: true
requirements: [GROW-01, GROW-04, PERF-01, PERF-02]
must_haves:
  truths:
    - "Visitors receive a responsive exhibition under documented transfer and frame-time targets on the representative devices."
    - "The owner can add ten content-driven exhibits while earlier URLs, ordering and poses remain stable."
    - "The scene stops work when idle, hidden or offscreen, and repeated visits retain stable resource/listener ownership."
    - "Release evidence distinguishes actual-device measurements from automated correctness and emulation."
  artifacts:
    - path: .planning/phases/03-growth-and-release-polish/03-PERFORMANCE-TARGETS.md
      provides: Exact targets established before measurements
    - path: scripts/measure-exhibition.mjs
      provides: Transfer and browser frame-interval evidence collection
    - path: tests/performance-release.spec.ts
      provides: Expanded-content and actual lifecycle stabilization checks
    - path: .planning/phases/03-growth-and-release-polish/03-MEASUREMENTS.md
      provides: Measured results, source/device provenance and honest pending fields
  key_links:
    - from: package.json
      to: scripts/check-project-growth.mjs
      via: Aggregate check includes isolated growth/media/performance coverage without replacing dist
    - from: scripts/measure-exhibition.mjs
      to: .planning/phases/03-growth-and-release-polish/03-MEASUREMENTS.md
      via: Recorded measurements tied to commit, device, viewport and evidence class
---

## Phase Goal

**As a** portfolio owner, **I want to** extend the exhibition with completed projects while preserving a polished, responsive experience, **so that** visitors can explore my growing work through a reliable live application link.

<objective>
Prove growth and lifecycle, establish and measure release budgets per D-05, D-16, D-24, D-25, D-26 and D-28. Physical evidence is required for PERF-01 acceptance; implementation may finish with verification human_needed when hardware is unavailable.
Purpose: Protect responsiveness as real projects accumulate without misreporting lab results as field performance.
Output: Bounded rendering, reproducible measurement tools, aggregate gates and evidence record.
</objective>

<execution_context>
@/home/castlewr/.codex/gsd-core/workflows/execute-plan.md
@/home/castlewr/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-growth-and-release-polish/03-CONTEXT.md
@.planning/phases/03-growth-and-release-polish/03-RESEARCH.md
@.planning/phases/03-growth-and-release-polish/03-06-SUMMARY.md
@.planning/phases/02-one-handed-surreal-exhibition/02-MEASUREMENTS.md
@.planning/phases/02-one-handed-surreal-exhibition/02-UAT.md
@scripts/measure-scene-budget.mjs
@src/scripts/exhibition/scene/quality.ts

quality.ts measures synchronous submission cost only. Keep its 28ms/12-sample degradation, 14ms/90-sample recovery and two 33ms reflection samples separate from complete-frame evidence. Existing actual GPU allocation ceiling is 5+(N-1), including one previously unattributed allocation; never revert to the disproven three-total estimate.
</context>

## Artifacts this phase produces

- New files: 03-PERFORMANCE-TARGETS.md, 03-MEASUREMENTS.md, scripts/measure-exhibition.mjs, tests/performance-release.spec.ts at the paths above.
- Measurement CLI: node scripts/measure-exhibition.mjs --base-url URL --label NAME --seconds 30 --output PATH; records navigation/resource transfer, animation-frame interval samples, render submission cost separately, viewport/DPR/browser/renderer and commit. JSON output is a test artifact, not public content.
- New texture-window API: exhibits.setActiveStation(station), consuming each panel's stable station and keeping current/adjacent texture work bounded.
- New check commands in package.json: check:growth and measure:exhibition; wire all new unit/browser suites into the existing check script without adding packages.
- New actual-allocation/listener instrumentation belongs in tests, with counters reflecting browser registrations and WebGL creation/deletion; no visitor-facing diagnostics panel.

<tasks>
<task type="auto">
  <name>Task 1: Set explicit performance targets and fail an expanded visitor/lifecycle contract</name>
  <read_first>tests/fixtures/projects/entries.mjs, .planning/phases/02-one-handed-surreal-exhibition/02-UI-SPEC.md, .planning/phases/02-one-handed-surreal-exhibition/02-MEASUREMENTS.md, src/scripts/exhibition/scene/quality.ts, tests/exhibition-resilience.spec.ts, scripts/check-project-growth.mjs</read_first>
  <files>.planning/phases/03-growth-and-release-polish/03-PERFORMANCE-TARGETS.md, tests/performance-release.spec.ts, scripts/measure-exhibition.mjs, tests/fixtures/projects/entries.mjs</files>
  <action>Before measuring, write these delegated Phase 3 design targets per D-26, explicitly distinguishing them from historical approval: cold essential still-view transfer ≤1,000,000 bytes; cold optional-3D startup including essential bytes ≤3,000,000; full N=10 home traversal transfer ≤3,000,000; report warm transfers separately. On the actual Windows laptop/Chrome at normal power and 60Hz, continuous-travel complete-frame median ≤16.7ms, p95 ≤33.4ms and worst ≤100ms. On physical iPhone 12 Pro/Safari after explicit 3D opt-in, median ≤33.4ms, p95 ≤50ms and worst ≤100ms. Sample at least three forward/back traversals after first usable presentation, record cold startup separately, and retain failed runs. Preserve inherited scene gzip ≤190,000B; previews ≤180,000B/1600px; draws ≤90+12(N−1), triangles ≤120000+18000(N−1), materials ≤18+(N−1), lines ≤1200+400(N−1), actual textures ≤5+(N−1); for N=0 use the N=1 ceiling. Preserve one reflection pass, 1024×512 target, one shadow light, 1024²/512² shadows and DPR caps 2/1.75/1.25. Write an end-to-end N=10 contract for all routes/picks/returns, stable earlier poses/order, one-second idle and independent hidden/offscreen cancellation, ten still/3D remounts and five project visits. Instrument actual EventTarget registration/removal and actual WebGL allocations/context release, not hardcoded ownership claims. Add the measurement CLI with honest rAF interval/transfer collection; label headless results lab and submission timing separately. Make all ten fixture preview assets distinct: derive separate image files from permitted evidence with deterministic fixture-only annotations and representative screenshot/terminal/diagram detail and ratios. Require ten distinct optimized preview URLs/hashes rather than one cached shared image; report unique asset count, encoded bytes, dimensions and actual transfer. Retain the image-size limits and keep these files in the temporary fixture build. Capture the pre-fix failure or measured resource/transfer risk before optimizing.</action>
  <acceptance_criteria>Targets are written before result collection and remain fixed throughout this plan. Tests observe actual listener/resource growth and request/render work; an empty or missing sample cannot count as a performance pass.</acceptance_criteria>
  <verify><automated>npm run test:e2e -- tests/performance-release.spec.ts --workers=1</automated></verify>
  <done>The release has concrete budgets and reproducible behavior/evidence checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep expanded travel and repeated visits within bounded owned work</name>
  <read_first>src/scripts/exhibition/scene/index.ts, src/scripts/exhibition/scene/exhibit.ts, src/scripts/exhibition/controller.ts, tests/exhibition-resilience.spec.ts, tests/performance-release.spec.ts</read_first>
  <files>src/scripts/exhibition/scene/index.ts, src/scripts/exhibition/scene/exhibit.ts, src/scripts/exhibition/controller.ts, tests/exhibition-resilience.spec.ts</files>
  <behavior>Distant images are not all eagerly decoded/uploaded at mount. Current/adjacent panels load on demand. Ten remounts and five project visits return to the same actual resource/listener baseline. Hidden/offscreen cancellation and idle suspension work independently.</behavior>
  <action>Implement bounded texture preparation per D-05/D-26: use station and stable panel IDs to prepare the current/adjacent exhibits, request a repaint after successful decode and dispose GPU maps outside that window, while retaining the DOM still and shared URL. Prevent late decode completion from allocating after teardown or after leaving the window. Keep failed-image descriptions/captions and picking functional. Fix any listener/observer/timer ownership exposed by the first task: remove the actual registered handler, cancel pending callbacks, and dispose targets/materials/geometries idempotently after partial initialization, still switches and page lifecycle transitions. Preserve BFCache restoration and avoid duplicating the controller on pageshow. Compare warmed identical stops/quality after each remount; allow no upward drift, and require scene-owned registrations to return to pre-mount baseline after teardown. Retain submission-based quality fallback, real allocation counts and the unattributed allocation disclosure. Optimize batching/visibility or assets within existing limits, without hiding evidence, weakening checks or introducing an animation loop.</action>
  <acceptance_criteria>No monotonic listener or allocation growth occurs through the required cycles; context-owned resources are released on teardown. A hidden tab and an offscreen exhibition each independently cancel work. Idle renderCount stays unchanged after one second.</acceptance_criteria>
  <verify><automated>npm run test:e2e -- tests/performance-release.spec.ts tests/exhibition-resilience.spec.ts --grep "resource|listener|idle|hidden|offscreen|remount|visit|texture" --workers=1</automated></verify>
  <done>Growth and repeated visits preserve responsive, bounded browser work.</done>
</task>

<task type="auto">
  <name>Task 3: Measure real and expanded releases and wire complete automated gates</name>
  <read_first>scripts/measure-scene-budget.mjs, scripts/check-project-growth.mjs, package.json, tests/performance-release.spec.ts, .planning/phases/03-growth-and-release-polish/03-PERFORMANCE-TARGETS.md, .github/workflows/pages.yml</read_first>
  <files>scripts/measure-scene-budget.mjs, scripts/check-project-growth.mjs, package.json, tests/performance-release.spec.ts, .planning/phases/03-growth-and-release-polish/03-MEASUREMENTS.md</files>
  <action>Extend artifact measurement to all published previews and report controller, scene, essential/full transfer separately. Keep the source motion-clock guard but update its explanatory wording to route station. Wire every new Node/browser suite from 03-01 through 03-07 into npm run check and implement check:growth running isolated N=0,1,10 growth/media/performance cases; the real production N=2 runs against the normal checked dist. Fixture builds must not replace or mutate dist before Pages upload. Compare earlier slugs/order/frame samples between single and expanded scenarios, test per-exhibit actual picks/returns, and run both reflection modes within N-based limits. Record all measured medians/p95/worst, transfer/cache status, quality, listener/resource baselines, device/browser/OS/GPU, viewport/DPR, SHA and commands in 03-MEASUREMENTS. Headless rAF data is a browser scheduling proxy, not proof of displayed GPU frame time. Request the independent actual-device trace/checklist evidence through the normal end-of-phase handoff: Windows Chrome Performance trace and physical iPhone Safari rendering-frame/timeline observations must substantiate complete-frame budgets on both real N=2 and the locally served isolated N=10 build. Serve the fixture temporarily on the test network without publishing it. Preserve all earlier pending UAT results. If devices are unavailable, leave those fields pending, identify PERF-01 as unverified and final verification as human_needed; recording pending evidence does not satisfy the observable performance truth.</action>
  <acceptance_criteria>Aggregate checks include every new suite and pass with unchanged production dist after fixtures. Measurements distinguish lab, physical and pending evidence. PERF-01 can be marked satisfied only when both actual target devices meet the recorded thresholds for real and expanded content.</acceptance_criteria>
  <verify><automated>npm run check</automated><human-check>Independently collect the Windows Chrome and physical iPhone Safari timing, usability/zoom and expanded-content results using the short release checklist. Enter actual device/browser/version and evidence paths; pending remains pending.</human-check></verify>
  <done>Automated growth/lifecycle evidence and all available measurements are recorded; outstanding physical evidence is explicit and blocks performance acceptance, not completion of implementation work.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| More content → browser resources | Growth can exhaust memory or transfer. |
| Temporary fixture → production artifact | Test content must never replace the checked release. |
| Lab measurements → release claims | Evidence class must remain accurate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-16 | Denial of service | images/scene lifecycle | mitigate | Adjacent-only GPU window, actual ownership checks, idle/visibility cancellation and fixed transfer/resource ceilings. |
| T-03-17 | Information disclosure | fixture builds | mitigate | Temporary copies, unchanged main dist hashes and production sentinel scan. |
| T-03-18 | Repudiation | measurement record | mitigate | Device/SHA provenance, separate submission/rAF/display evidence and pending physical results. |
</threat_model>

<verification>Run the complete aggregate check once after changes, then collect production N=2 and isolated N=10 measurements. Repeat only failed or changed checks. Do not raise targets to fit results or treat Chromium emulation as Safari/physical evidence.</verification>

<success_criteria>Automated growth and lifecycle checks pass within inherited budgets. PERF-01 acceptance additionally requires passing actual Windows/iPhone complete-device and transfer evidence; without it, retain human_needed.</success_criteria>

<output>Create .planning/phases/03-growth-and-release-polish/03-07-SUMMARY.md identifying exactly which requirements are verified and which physical results remain pending.</output>
