---
phase: 03-growth-and-release-polish
plan: "08"
type: execute
wave: 6
depends_on: ["03-07"]
files_modified:
  - tests/production-smoke.test.mjs
  - scripts/production-smoke.mjs
  - README.md
  - docs/RELEASE.md
  - package.json
  - src/content/projects/portfolio/index.md
  - .planning/phases/03-growth-and-release-polish/03-RELEASE-EVIDENCE.md
autonomous: true
requirements: [SHIP-03, SHIP-04, PERF-01]
must_haves:
  truths:
    - "The owner can publish a checked update and verify actual live HTTPS routes, assets, navigation and contact destinations."
    - "The owner can restore a known-good release through a normal revert and verify restored live content."
    - "The intended release is restored after the recovery drill."
    - "The owner has a concise independent checklist and an accurate record of remaining physical-device acceptance."
  artifacts:
    - path: README.md
      provides: Short project entry point and useful commands
    - path: docs/RELEASE.md
      provides: Publishing, smoke, recovery and independent visitor checklist
    - path: scripts/production-smoke.mjs
      provides: Actual-host HTTP/artifact and browser smoke runner
      exports: [runProductionSmoke]
    - path: .planning/phases/03-growth-and-release-polish/03-RELEASE-EVIDENCE.md
      provides: SHA/run/artifact/deployment/live evidence for release and exercised recovery
  key_links:
    - from: docs/RELEASE.md
      to: .github/workflows/pages.yml
      via: Existing master-only checked-dist build/upload/deploy contract
    - from: scripts/production-smoke.mjs
      to: https://castlew640.github.io/
      via: Actual HTTPS route/asset checks plus native visitor journeys
---

## Phase Goal

**As a** portfolio owner, **I want to** extend the exhibition with completed projects while preserving a polished, responsive experience, **so that** visitors can explore my growing work through a reliable live application link.

<objective>
Deliver and exercise the concise release handoff per D-24, D-25 and D-26 while preserving D-09/D-28 truthful claims and checked-artifact publishing. Keep physical acceptance and earlier pending UAT visible.
Purpose: The application link must remain checkable and recoverable during the owner's job search.
Output: Short runbooks, repeatable live smoke, exercised ordinary-revert recovery and evidence.
</objective>

<execution_context>
@/home/castlewr/.codex/gsd-core/workflows/execute-plan.md
@/home/castlewr/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-growth-and-release-polish/03-CONTEXT.md
@.planning/phases/03-growth-and-release-polish/03-RESEARCH.md
@.planning/phases/03-growth-and-release-polish/03-07-SUMMARY.md
@.planning/phases/03-growth-and-release-polish/03-MEASUREMENTS.md
@.planning/phases/02-one-handed-surreal-exhibition/02-UAT.md
@.planning/phases/01-publishable-portfolio-and-delivery/01-03-SUMMARY.md
@.github/workflows/pages.yml

The existing destination is castlew640/castlew640.github.io, master, GitHub Pages at https://castlew640.github.io/. Build has contents:read; deploy alone has pages:write/id-token:write and needs:build. It uploads the already checked dist and never rebuilds in deploy. The earlier failed-check drill proved preservation; this task must observe a previously deployed version actually being restored.
</context>

## Artifacts this phase produces

- New files: tests/production-smoke.test.mjs, scripts/production-smoke.mjs, README.md, docs/RELEASE.md and 03-RELEASE-EVIDENCE.md.
- New API/CLI: runProductionSmoke({baseUrl, distRoot, outputPath}); node scripts/production-smoke.mjs --base-url URL --dist DIST --output REPORT.json. HTTPS is required except loopback test servers. Reports are evidence artifacts outside public.
- New package script: smoke:production; include smoke unit tests in check. No new package, host, credentials or workflow permissions.
- One accurate sentence in the portfolio case study is the reviewed reversible release delta used to prove actual restoration; the intended final sentence remains after the drill.

<tasks>
<task type="auto">
  <name>Task 1: Define a failing smoke journey against real artifact expectations</name>
  <read_first>scripts/verify-built-content.mjs, .github/workflows/pages.yml, tests/portfolio.spec.ts, src/data/profile.ts, .planning/phases/03-growth-and-release-polish/03-MEASUREMENTS.md</read_first>
  <files>tests/production-smoke.test.mjs</files>
  <action>Write tests for the new smoke runner per SHIP-03/D-24 using a controlled local HTTP server and the actual built artifact contract. Require failure for non-HTTPS remote origins, missing/direct-route fallback HTML, wrong image/PDF bytes or content type, stale page hashes and broken return/contact destinations. Permit loopback HTTP only for tests. The browser portion must visit/refresh every published project route, use each exhibit return, reach resume/contact, verify fresh-phone still mode and explicit 3D opt-in, and cover ordinary HTML when JS is disabled. External contact links are inspected, not activated to send mail or submit forms. Capture the initial missing-runner failure before implementing it.</action>
  <acceptance_criteria>The test distinguishes a healthy checked artifact from a 200-status error/fallback page and from a stale deployment.</acceptance_criteria>
  <verify><automated>node --test tests/production-smoke.test.mjs</automated></verify>
  <done>A meaningful live-site smoke contract exists before automation is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Automate production smoke and write the concise owner runbooks</name>
  <read_first>tests/production-smoke.test.mjs, scripts/verify-built-content.mjs, package.json, docs/AUTHORING.md, .github/workflows/pages.yml, .planning/phases/03-growth-and-release-polish/03-PERFORMANCE-TARGETS.md</read_first>
  <files>scripts/production-smoke.mjs, README.md, docs/RELEASE.md, package.json</files>
  <behavior>The smoke runner checks HTTPS, actual emitted routes/assets/PDF bytes, canonical navigation and expected contact links; any mismatch produces a nonzero result. Runbooks provide commands and expected outcomes without a guided review session.</behavior>
  <action>Implement the runner with native fetch, node:crypto and existing Playwright per D-24/D-25. Enumerate published routes/assets from the checked dist; compare decoded response SHA-256 against local HTML, CSS/JS, images, and resume PDF, checking status/content type and PDF signature. Include poster/video/VTT if actually used. Follow redirects only when the final origin/protocol remain expected. Drive the native browser journeys from Task 1, including every project and fresh phone default/opt-in. Bound request timeouts, report failures clearly and exit nonzero. Add smoke:production and its Node tests to the existing check command. Keep README to roughly 40 lines linking authoring/release guides and naming dev/check/build/preview commands, stack and public destination. Keep RELEASE concise with npm run check, normal commit/push, gh run view/watch, smoke command and expected evidence fields; retain the exact checked-dist/least-privilege workflow with no destination changes. Include a short independent checklist for Windows Chrome mouse/keyboard/200–400% real zoom, physical iPhone Safari still/opt-in/touch/pinch/browser-chrome behavior, project/return/resume/contact, composition/reflections and the separate performance evidence protocol. Link pending prior UAT and 03-MEASUREMENTS rather than asserting they passed.</action>
  <acceptance_criteria>Smoke tests pass and the runner rejects controlled stale/missing responses. Documentation is executable, brief and matches the real branch/host. Existing workflow pins, build/deploy dependency and permissions are unchanged.</acceptance_criteria>
  <verify><automated>node --test tests/production-smoke.test.mjs</automated></verify>
  <done>The owner can independently publish and verify the live visitor journey with concise commands and checks.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the live release and exercise restoration with normal revert commits</name>
  <read_first>docs/RELEASE.md, scripts/production-smoke.mjs, src/content/projects/portfolio/index.md, .planning/STATE.md, .planning/phases/03-growth-and-release-polish/03-MEASUREMENTS.md, .github/workflows/pages.yml</read_first>
  <files>src/content/projects/portfolio/index.md, docs/RELEASE.md, .planning/phases/03-growth-and-release-polish/03-RELEASE-EVIDENCE.md</files>
  <action>Finish the committed reviewable release and successful local checks first. Honor the current session's publication authorization; do not seek it again when it already covers publishing and the controlled recovery exercise. If authorization is narrower, present the concrete candidate SHA and exact remaining live actions after all reversible preparation, leaving actual release evidence pending until authorized. Publish through the existing master workflow, correlate source SHA/run URL/artifact ID/deployment ID/time, then download that successful run's github-pages artifact with gh run download, extract artifact.tar into a run-specific checked-dist directory and execute production smoke with --dist pointing there. Compare live bytes to the downloaded CI artifact, not an assumed byte-identical local rebuild. Record this observed healthy release as known-good SHA K and retain its downloaded artifact plus a SHA-256 manifest throughout the drill. Create one ordinary, reviewed content commit C adding the accurate case-study decision sentence “The view switch preserves the current exhibit.”, deploy successfully and verify the sentence live. Use git revert C, commit/push normally, await the checked pipeline and prove live page/asset hashes and the sentence's absence match the known-good content. Then git revert the recovery commit, deploy and prove the intended C content is restored. Extract the final intended run's artifact under /tmp/phase03-release/intended/checked-dist for the final verify command. Rebuild/check each candidate; download/extract each successful CI run's artifact and smoke against that exact output, using K's retained manifest to prove restored public content; capture new run/artifact/deployment evidence; never reset, force-push, fabricate a failing live release or depend on expired artifacts. Record exact commit IDs, commands, live response evidence and elapsed restoration in 03-RELEASE-EVIDENCE and make the runbook's recovery example concrete. Resolve smoke failures before calling the release healthy. Do not mark PERF-01 or earlier human UAT complete without their actual evidence, and do not add public recovery/performance claims beyond what this drill demonstrates.</action>
  <acceptance_criteria>Live HTTPS/routes/assets/PDF/contact/navigation checks pass for the intended release. The recorded known-good content is actually observed after revert, and intended content is observed again after the final restoration. Missing live authorization/device evidence is plainly pending and never a passing SHIP-04/PERF-01 result.</acceptance_criteria>
  <verify><automated>node scripts/production-smoke.mjs --base-url https://castlew640.github.io/ --dist /tmp/phase03-release/intended/checked-dist --output /tmp/phase03-production-smoke.json</automated><human-check>Owner independently completes the short device/visitor checklist in docs/RELEASE.md. Carry unresolved physical timing, iPhone behavior and previous UAT forward as human_needed.</human-check></verify>
  <done>The selected host has current smoke evidence and exercised durable recovery, with intended content restored; any unavailable physical acceptance remains explicit.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Checked dist → GitHub Pages | Only the checked artifact should become public. |
| Git history → recovery | Restoring a release must preserve history and the intended final state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-19 | Tampering | Pages pipeline | mitigate | Preserve immutable action pins, master-only deploy, needs:build and artifact hash comparison. |
| T-03-20 | Elevation of privilege | CI permissions | mitigate | Read-only build; Pages/OIDC permissions confined to the existing deploy job. |
| T-03-21 | Repudiation | recovery record | mitigate | Record K/C/revert/restoration SHAs and live content evidence for each checked deployment. |
| T-03-22 | Information disclosure | smoke output | mitigate | Log public URLs/status/hashes only; inspect contact destinations without sending messages. |
</threat_model>

<verification>Run aggregate checks and smoke unit tests before publication. Verify the actual live artifact after initial release, recovery and final restoration. SHIP-04 requires the observed drill; PERF-01 requires physical measurements from 03-07. Documentation alone does not pass either requirement.</verification>

<success_criteria>The live application link is verified, rollback is documented and exercised, the intended release is restored, and the owner receives a short independent checklist with honest outstanding acceptance.</success_criteria>

<output>Create .planning/phases/03-growth-and-release-polish/03-08-SUMMARY.md with run/evidence links, restored intended SHA and requirement status.</output>
