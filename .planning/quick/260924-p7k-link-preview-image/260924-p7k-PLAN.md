# Quick Task 260924-p7k: Link preview image

**Requested:** 2026-09-24 by the owner: add a preview image to the site, and mark phone and Windows testing complete.

## Task

- Publish `public/og-image.jpg`, a 1200×630 still of the entrance (name card over the checkered path, melting clocks and framed project screens).
- Add Open Graph image tags (`og:image`, type, width, height, alt, `og:site_name`) and `twitter:card=summary_large_image` to `BaseLayout.astro`, so every page shares the same preview.
- Extend `scripts/verify-built-content.mjs` so the build fails if the image is missing, is not a 1200×630 JPEG, or any page lacks the tags.
- Record the owner's completed Windows and phone testing in the Phase 2 and Phase 3 UAT, verification, measurement, requirement, roadmap and state records.

## Verification

- `astro check` has no errors; `npm run build` and `node scripts/verify-built-content.mjs` pass.
- Built `index.html` carries the absolute `https://castlew640.github.io/og-image.jpg` URL and the large-card tag.
